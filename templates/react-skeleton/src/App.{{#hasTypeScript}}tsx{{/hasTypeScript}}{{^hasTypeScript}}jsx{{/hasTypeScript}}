{{#hasTypeScript}}import React from 'react';{{/hasTypeScript}}{{^hasTypeScript}}import React from 'react';{{/hasTypeScript}}
{{#hasRouter}}import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';{{/hasRouter}}
{{#hasRedux}}import { Provider } from 'react-redux';
import { store } from './store';{{/hasRedux}}
{{#hasReactQuery}}import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';{{/hasReactQuery}}
{{#isMaterialUI}}import { ThemeProvider, createTheme } from '@mui/material/styles';
import CssBaseline from '@mui/material/CssBaseline';{{/isMaterialUI}}
{{#isAntDesign}}import { ConfigProvider } from 'antd';{{/isAntDesign}}
import './App.css';

{{#hasReactQuery}}
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000, // 5 minutes
      retry: 1,
    },
  },
});
{{/hasReactQuery}}

{{#isMaterialUI}}
const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});
{{/isMaterialUI}}

{{#hasTypeScript}}
interface AppProps {}

const App: React.FC<AppProps> = () => {
{{/hasTypeScript}}
{{^hasTypeScript}}
function App() {
{{/hasTypeScript}}
  return (
    {{#hasRedux}}
    <Provider store={store}>
    {{/hasRedux}}
      {{#hasReactQuery}}
      <QueryClientProvider client={queryClient}>
      {{/hasReactQuery}}
        {{#isMaterialUI}}
        <ThemeProvider theme={theme}>
          <CssBaseline />
        {{/isMaterialUI}}
        {{#isAntDesign}}
        <ConfigProvider>
        {{/isAntDesign}}
          {{#hasRouter}}
          <Router>
          {{/hasRouter}}
            <div className="App">
              <header className="App-header">
                <h1>{{appNamePascalCase}}</h1>
                <p>Enterprise React Application</p>
                {{#hasAuth}}
                <p>Authentication: Enabled</p>
                {{/hasAuth}}
                {{#hasRedux}}
                <p>State Management: Redux Toolkit</p>
                {{/hasRedux}}
                {{#hasRouter}}
                <p>Routing: React Router v6</p>
                {{/hasRouter}}
                {{#isMaterialUI}}
                <p>UI Framework: Material-UI</p>
                {{/isMaterialUI}}
                {{#isAntDesign}}
                <p>UI Framework: Ant Design</p>
                {{/isAntDesign}}
              </header>

              {{#hasRouter}}
              <main>
                <Routes>
                  <Route path="/" element={<HomePage />} />
                  <Route path="/about" element={<AboutPage />} />
                  {{#hasAuth}}
                  <Route path="/login" element={<LoginPage />} />
                  <Route path="/dashboard" element={<DashboardPage />} />
                  {{/hasAuth}}
                </Routes>
              </main>
              {{/hasRouter}}
              {{^hasRouter}}
              <main>
                <p>
                  Generated by Enterprise CLI on {{timestamp}}
                </p>
                <p>
                  Backend API: {{apiBaseUrl}}
                </p>
              </main>
              {{/hasRouter}}
            </div>
          {{#hasRouter}}
          </Router>
          {{/hasRouter}}
        {{#isAntDesign}}
        </ConfigProvider>
        {{/isAntDesign}}
        {{#isMaterialUI}}
        </ThemeProvider>
        {{/isMaterialUI}}
        {{#hasReactQuery}}
        <ReactQueryDevtools initialIsOpen={false} />
      </QueryClientProvider>
      {{/hasReactQuery}}
    {{#hasRedux}}
    </Provider>
    {{/hasRedux}}
  );
{{#hasTypeScript}}
};
{{/hasTypeScript}}
{{^hasTypeScript}}
}
{{/hasTypeScript}}

{{#hasRouter}}
// Sample page components
const HomePage = () => (
  <div>
    <h2>Home Page</h2>
    <p>Welcome to {{appNamePascalCase}}</p>
  </div>
);

const AboutPage = () => (
  <div>
    <h2>About Page</h2>
    <p>This is an enterprise React application built with the Enterprise CLI.</p>
  </div>
);

{{#hasAuth}}
const LoginPage = () => (
  <div>
    <h2>Login</h2>
    <p>Authentication will be implemented here.</p>
  </div>
);

const DashboardPage = () => (
  <div>
    <h2>Dashboard</h2>
    <p>Protected dashboard content.</p>
  </div>
);
{{/hasAuth}}
{{/hasRouter}}

export default App;