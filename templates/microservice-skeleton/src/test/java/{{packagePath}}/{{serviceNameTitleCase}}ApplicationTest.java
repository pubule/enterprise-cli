package {{packageName}};

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

/**
 * Simple application startup test
 *
 * Verifies that the Spring Boot application context loads successfully.
 * Generated by Enterprise CLI.
 */
@SpringBootTest(classes = {{serviceNameTitleCase}}Application.class)
@ActiveProfiles("test")
@DisplayName("{{serviceNameTitleCase}} Application Tests")
class {{serviceNameTitleCase}}ApplicationTest {

    @Test
    @DisplayName("Should load application context successfully")
    void shouldLoadApplicationContextSuccessfully() {
        // This test verifies that the Spring Boot application context loads without errors.
        // If this test passes, it means:
        // - All @Component, @Service, @Repository beans are properly configured
        // - Application configuration is valid
        // - Dependencies are correctly wired
        // - Database configuration is working (with test profile)
    }
}