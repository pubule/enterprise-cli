package {{packageName}}.exception;

import org.springframework.http.HttpStatus;

/**
 * Exception for resource not found scenarios
 *
 * Used when a requested resource (entity, file, etc.) cannot be found.
 * Generated by Enterprise CLI - customize according to your business requirements.
 */
public class ResourceNotFoundException extends BusinessException {

    public ResourceNotFoundException(String message) {
        super(message, HttpStatus.NOT_FOUND, "RESOURCE_NOT_FOUND");
    }

    public ResourceNotFoundException(String resourceType, String identifier) {
        super(String.format("%s with identifier '%s' not found", resourceType, identifier),
                HttpStatus.NOT_FOUND, "RESOURCE_NOT_FOUND");
    }

    public ResourceNotFoundException(String message, Throwable cause) {
        super(message, cause, HttpStatus.NOT_FOUND, "RESOURCE_NOT_FOUND");
    }

    /**
     * Convenience method for {{domainTitleCase}} not found
     */
    public static ResourceNotFoundException {{domain}}NotFound(String id) {
        return new ResourceNotFoundException("{{domainTitleCase}}", id);
    }

    /**
     * Convenience method for {{domainTitleCase}} not found by name
     */
    public static ResourceNotFoundException {{domain}}NotFoundByName(String name) {
        return new ResourceNotFoundException(String.format("{{domainTitleCase}} with name '%s' not found", name));
    }
}