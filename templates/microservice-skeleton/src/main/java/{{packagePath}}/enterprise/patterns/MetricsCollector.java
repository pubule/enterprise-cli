package {{packageName}}.enterprise.patterns;

import io.micrometer.core.instrument.Timer;

/**
 * 🤖 ENTERPRISE INFRASTRUCTURE - DO NOT MODIFY
 *
 * Metrics Collector interface for enterprise patterns
 * Enables easy mocking in unit tests while maintaining
 * production functionality.
 *
 * Generated by Enterprise CLI - Infrastructure layer
 */
public interface MetricsCollector {

    // Timer methods
    Timer.Sample startCreationTimer(Class<?> entityClass);
    void stopCreationTimer(Timer.Sample sample, Class<?> entityClass);

    Timer.Sample startReadTimer(Class<?> entityClass);
    void stopReadTimer(Timer.Sample sample, Class<?> entityClass);

    Timer.Sample startUpdateTimer(Class<?> entityClass);
    void stopUpdateTimer(Timer.Sample sample, Class<?> entityClass);

    Timer.Sample startDeleteTimer(Class<?> entityClass);
    void stopDeleteTimer(Timer.Sample sample, Class<?> entityClass);

    Timer.Sample startListTimer(Class<?> entityClass);
    void stopListTimer(Timer.Sample sample, Class<?> entityClass);

    Timer.Sample startSearchTimer(Class<?> entityClass);
    void stopSearchTimer(Timer.Sample sample, Class<?> entityClass);

    // Success metrics
    void recordCreation(Class<?> entityClass);
    void recordRead(Class<?> entityClass);
    void recordUpdate(Class<?> entityClass);
    void recordDeletion(Class<?> entityClass);
    void recordList(Class<?> entityClass, int resultCount);
    void recordSearch(Class<?> entityClass);

    // Error metrics
    void recordValidationError(Class<?> entityClass);
    void recordSystemError(Class<?> entityClass);
    void recordBusinessRuleViolation(Class<?> entityClass, String ruleType);

    // Business metrics
    void recordBusinessMetric(String metricName, double value, String... tags);
    void recordBusinessEvent(String eventType, String entityType);

    // Cache metrics
    void recordCacheHit(Class<?> entityClass);
    void recordCacheMiss(Class<?> entityClass);

    // Summary
    EnterpriseMetricsCollector.MetricsSummary getMetricsSummary(Class<?> entityClass);
}