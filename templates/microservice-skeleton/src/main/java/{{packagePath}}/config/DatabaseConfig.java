package {{packageName}}.config;

import com.zaxxer.hikari.HikariConfig;
import com.zaxxer.hikari.HikariDataSource;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Primary;
import org.springframework.context.annotation.Profile;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;
import org.springframework.data.jpa.repository.config.EnableJpaRepositories;
import org.springframework.orm.jpa.JpaTransactionManager;
import org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean;
import org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter;
import org.springframework.transaction.PlatformTransactionManager;
import org.springframework.transaction.annotation.EnableTransactionManagement;

import javax.sql.DataSource;
import java.util.Properties;

/**
 * Database Configuration for {{serviceNameTitleCase}}
 *
 * Configures database connections, JPA settings, and transaction management.
 * Optimized for enterprise performance and reliability.
 * Generated by Enterprise CLI - customize according to your database requirements.
 */
@Configuration
@EnableJpaRepositories(basePackages = "{{packageName}}.repository")
@EnableJpaAuditing
@EnableTransactionManagement
public class DatabaseConfig {

    @Value("${spring.datasource.url}")
    private String databaseUrl;

    @Value("${spring.datasource.username}")
    private String databaseUsername;

    @Value("${spring.datasource.password}")
    private String databasePassword;

    @Value("${spring.datasource.driver-class-name}")
    private String driverClassName;

    {{#postgresql}}
    /**
     * Production-optimized DataSource for PostgreSQL
     */
    @Bean
    @Primary
    @Profile("!test")
    public DataSource productionDataSource() {
        HikariConfig config = new HikariConfig();

        // Connection settings
        config.setJdbcUrl(databaseUrl);
        config.setUsername(databaseUsername);
        config.setPassword(databasePassword);
        config.setDriverClassName(driverClassName);

        // Pool settings optimized for PostgreSQL
        config.setMaximumPoolSize(20);
        config.setMinimumIdle(5);
        config.setConnectionTimeout(20000); // 20 seconds
        config.setIdleTimeout(300000); // 5 minutes
        config.setMaxLifetime(1200000); // 20 minutes
        config.setLeakDetectionThreshold(60000); // 1 minute

        // PostgreSQL-specific optimizations
        config.addDataSourceProperty("cachePrepStmts", "true");
        config.addDataSourceProperty("prepStmtCacheSize", "250");
        config.addDataSourceProperty("prepStmtCacheSqlLimit", "2048");
        config.addDataSourceProperty("useServerPrepStmts", "true");
        config.addDataSourceProperty("useLocalSessionState", "true");
        config.addDataSourceProperty("rewriteBatchedStatements", "true");
        config.addDataSourceProperty("cacheResultSetMetadata", "true");
        config.addDataSourceProperty("cacheServerConfiguration", "true");
        config.addDataSourceProperty("elideSetAutoCommits", "true");
        config.addDataSourceProperty("maintainTimeStats", "false");

        // Connection validation
        config.setConnectionTestQuery("SELECT 1");
        config.setValidationTimeout(5000);

        // Pool name for monitoring
        config.setPoolName("{{serviceNameTitleCase}}-HikariCP");

        return new HikariDataSource(config);
    }
    {{/postgresql}}

    {{#mysql}}
    /**
     * Production-optimized DataSource for MySQL
     */
    @Bean
    @Primary
    @Profile("!test")
    public DataSource productionDataSource() {
        HikariConfig config = new HikariConfig();

        // Connection settings
        config.setJdbcUrl(databaseUrl);
        config.setUsername(databaseUsername);
        config.setPassword(databasePassword);
        config.setDriverClassName(driverClassName);

        // Pool settings optimized for MySQL
        config.setMaximumPoolSize(20);
        config.setMinimumIdle(5);
        config.setConnectionTimeout(20000); // 20 seconds
        config.setIdleTimeout(300000); // 5 minutes
        config.setMaxLifetime(1200000); // 20 minutes
        config.setLeakDetectionThreshold(60000); // 1 minute

        // MySQL-specific optimizations
        config.addDataSourceProperty("cachePrepStmts", "true");
        config.addDataSourceProperty("prepStmtCacheSize", "250");
        config.addDataSourceProperty("prepStmtCacheSqlLimit", "2048");
        config.addDataSourceProperty("useServerPrepStmts", "true");
        config.addDataSourceProperty("useLocalSessionState", "true");
        config.addDataSourceProperty("rewriteBatchedStatements", "true");
        config.addDataSourceProperty("cacheResultSetMetadata", "true");
        config.addDataSourceProperty("cacheServerConfiguration", "true");
        config.addDataSourceProperty("elideSetAutoCommits", "true");
        config.addDataSourceProperty("maintainTimeStats", "false");

        // Connection validation
        config.setConnectionTestQuery("SELECT 1");
        config.setValidationTimeout(5000);

        // Pool name for monitoring
        config.setPoolName("{{serviceNameTitleCase}}-HikariCP");

        return new HikariDataSource(config);
    }
    {{/mysql}}

    {{#h2}}
    /**
     * Development-optimized DataSource for H2
     */
    @Bean
    @Primary
    @Profile("!test")
    public DataSource developmentDataSource() {
        HikariConfig config = new HikariConfig();

        // Connection settings
        config.setJdbcUrl(databaseUrl);
        config.setUsername(databaseUsername);
        config.setPassword(databasePassword);
        config.setDriverClassName(driverClassName);

        // Pool settings optimized for development
        config.setMaximumPoolSize(10);
        config.setMinimumIdle(2);
        config.setConnectionTimeout(10000); // 10 seconds
        config.setIdleTimeout(180000); // 3 minutes
        config.setMaxLifetime(600000); // 10 minutes

        // H2-specific settings
        config.addDataSourceProperty("cachePrepStmts", "true");
        config.addDataSourceProperty("prepStmtCacheSize", "100");

        // Connection validation
        config.setConnectionTestQuery("SELECT 1");
        config.setValidationTimeout(3000);

        // Pool name for monitoring
        config.setPoolName("{{serviceNameTitleCase}}-HikariCP-Dev");

        return new HikariDataSource(config);
    }
    {{/h2}}

    /**
     * Test DataSource configuration
     */
    @Bean
    @Profile("test")
    public DataSource testDataSource() {
        HikariConfig config = new HikariConfig();

        // Test database connection
        config.setJdbcUrl("jdbc:h2:mem:{{serviceName}}_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE");
        config.setUsername("sa");
        config.setPassword("");
        config.setDriverClassName("org.h2.Driver");

        // Minimal pool settings for tests
        config.setMaximumPoolSize(5);
        config.setMinimumIdle(1);
        config.setConnectionTimeout(5000);

        // Pool name for monitoring
        config.setPoolName("{{serviceNameTitleCase}}-Test-HikariCP");

        return new HikariDataSource(config);
    }

    /**
     * Entity Manager Factory configuration
     */
    @Bean
    public LocalContainerEntityManagerFactoryBean entityManagerFactory(DataSource dataSource) {
        LocalContainerEntityManagerFactoryBean em = new LocalContainerEntityManagerFactoryBean();
        em.setDataSource(dataSource);
        em.setPackagesToScan("{{packageName}}.entity");

        HibernateJpaVendorAdapter vendorAdapter = new HibernateJpaVendorAdapter();
        em.setJpaVendorAdapter(vendorAdapter);

        // JPA properties
        Properties jpaProperties = new Properties();

        {{#postgresql}}
        jpaProperties.put("hibernate.dialect", "org.hibernate.dialect.PostgreSQLDialect");
        {{/postgresql}}
        {{#mysql}}
        jpaProperties.put("hibernate.dialect", "org.hibernate.dialect.MySQLDialect");
        {{/mysql}}
        {{#h2}}
        jpaProperties.put("hibernate.dialect", "org.hibernate.dialect.H2Dialect");
        {{/h2}}

        // Performance optimizations
        jpaProperties.put("hibernate.jdbc.batch_size", "20");
        jpaProperties.put("hibernate.jdbc.fetch_size", "50");
        jpaProperties.put("hibernate.order_inserts", "true");
        jpaProperties.put("hibernate.order_updates", "true");
        jpaProperties.put("hibernate.batch_versioned_data", "true");

        // Connection handling
        jpaProperties.put("hibernate.connection.provider_disables_autocommit", "true");
        jpaProperties.put("hibernate.query.plan_cache_max_size", "2048");
        jpaProperties.put("hibernate.query.plan_parameter_metadata_max_size", "128");

        // Statistics and monitoring
        jpaProperties.put("hibernate.generate_statistics", "false");
        jpaProperties.put("hibernate.session.events.log.LOG_QUERIES_SLOWER_THAN_MS", "100");

        // Development vs Production settings
        jpaProperties.put("hibernate.format_sql", "${spring.jpa.properties.hibernate.format_sql:false}");
        jpaProperties.put("hibernate.show_sql", "${spring.jpa.show-sql:false}");
        jpaProperties.put("hibernate.use_sql_comments", "${spring.jpa.properties.hibernate.use_sql_comments:false}");

        em.setJpaProperties(jpaProperties);

        return em;
    }

    /**
     * Transaction Manager configuration
     */
    @Bean
    public PlatformTransactionManager transactionManager(LocalContainerEntityManagerFactoryBean entityManagerFactory) {
        JpaTransactionManager transactionManager = new JpaTransactionManager();
        transactionManager.setEntityManagerFactory(entityManagerFactory.getObject());

        // Transaction timeout (30 seconds)
        transactionManager.setDefaultTimeout(30);

        return transactionManager;
    }
}