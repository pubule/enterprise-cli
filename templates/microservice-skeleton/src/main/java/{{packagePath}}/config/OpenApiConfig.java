package {{packageName}}.config;

{{#hasOpenApi}}
import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Contact;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.info.License;
import io.swagger.v3.oas.models.security.SecurityRequirement;
import io.swagger.v3.oas.models.security.SecurityScheme;
import io.swagger.v3.oas.models.servers.Server;
import org.springdoc.core.models.GroupedOpenApi;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

import java.util.List;

/**
 * OpenAPI Configuration for {{serviceNameTitleCase}}
 *
 * Configures Swagger/OpenAPI documentation with security, versioning, and enterprise features.
 * Generated by Enterprise CLI - customize according to your API documentation requirements.
 */
@Configuration
public class OpenApiConfig {

    @Value("${spring.application.name}")
    private String applicationName;

    @Value("${server.servlet.context-path:/api/v1}")
    private String contextPath;

    @Value("${app.version:1.0.0}")
    private String applicationVersion;

    @Value("${app.description:Enterprise {{domainTitleCase}} Microservice}")
    private String applicationDescription;

    /**
     * Main OpenAPI configuration
     */
    @Bean
    public OpenAPI customOpenAPI() {
        return new OpenAPI()
                .info(apiInfo())
                .servers(apiServers())
                .components(apiComponents())
                .addSecurityItem(new SecurityRequirement().addList("bearerAuth"))
                .addSecurityItem(new SecurityRequirement().addList("basicAuth"));
    }

    /**
     * API Information
     */
    private Info apiInfo() {
        return new Info()
                .title("{{serviceNameTitleCase}} API")
                .description(buildApiDescription())
                .version(applicationVersion)
                .contact(apiContact())
                .license(apiLicense());
    }

    /**
     * API Contact Information
     */
    private Contact apiContact() {
        return new Contact()
                .name("API Support Team")
                .email("api-support@company.com")
                .url("https://company.com/support");
    }

    /**
     * API License Information
     */
    private License apiLicense() {
        return new License()
                .name("Apache 2.0")
                .url("https://www.apache.org/licenses/LICENSE-2.0");
    }

    /**
     * API Servers
     */
    private List<Server> apiServers() {
        return List.of(
                new Server()
                        .url("http://localhost:8080" + contextPath)
                        .description("Development Server"),
                new Server()
                        .url("https://api-dev.company.com" + contextPath)
                        .description("Development Environment"),
                new Server()
                        .url("https://api-staging.company.com" + contextPath)
                        .description("Staging Environment"),
                new Server()
                        .url("https://api.company.com" + contextPath)
                        .description("Production Environment")
        );
    }

    /**
     * Security Components
     */
    private Components apiComponents() {
        return new Components()
                .addSecuritySchemes("bearerAuth",
                        new SecurityScheme()
                                .type(SecurityScheme.Type.HTTP)
                                .scheme("bearer")
                                .bearerFormat("JWT")
                                .description("JWT Authorization header using the Bearer scheme"))
                .addSecuritySchemes("basicAuth",
                        new SecurityScheme()
                                .type(SecurityScheme.Type.HTTP)
                                .scheme("basic")
                                .description("Basic Authentication"));
    }

    /**
     * Public API Group - endpoints that don't require authentication
     */
    @Bean
    public GroupedOpenApi publicApi() {
        return GroupedOpenApi.builder()
                .group("public")
                .displayName("Public API")
                .pathsToMatch("/{{domain}}s/hello", "/actuator/health", "/actuator/info")
                .build();
    }

    /**
     * {{domainTitleCase}} API Group - main business endpoints
     */
    @Bean
    public GroupedOpenApi {{domain}}Api() {
        return GroupedOpenApi.builder()
                .group("{{domain}}s")
                .displayName("{{domainTitleCase}} Management API")
                .pathsToMatch("/{{domain}}s/**")
                .pathsToExclude("/{{domain}}s/hello")
                .build();
    }

    /**
     * Management API Group - actuator and monitoring endpoints
     */
    @Bean
    public GroupedOpenApi managementApi() {
        return GroupedOpenApi.builder()
                .group("management")
                .displayName("Management & Monitoring API")
                .pathsToMatch("/actuator/**")
                .pathsToExclude("/actuator/health", "/actuator/info")
                .build();
    }

    {{#hasCamel}}
    /**
     * Integration API Group - Camel routes and integration endpoints
     */
    @Bean
    public GroupedOpenApi integrationApi() {
        return GroupedOpenApi.builder()
                .group("integration")
                .displayName("Integration API")
                .pathsToMatch("/camel/**")
                .build();
    }
    {{/hasCamel}}

    /**
     * All APIs Group - complete API documentation
     */
    @Bean
    public GroupedOpenApi allApis() {
        return GroupedOpenApi.builder()
                .group("all")
                .displayName("Complete API")
                .pathsToMatch("/**")
                .build();
    }

    /**
     * Build comprehensive API description
     */
    private String buildApiDescription() {
        StringBuilder description = new StringBuilder();
        description.append(applicationDescription).append("\n\n");

        description.append("## Features\n");
        description.append("- **CRUD Operations**: Complete {{domainTitleCase}} management\n");
        description.append("- **Search & Filtering**: Advanced search capabilities\n");
        description.append("- **Pagination**: Efficient data retrieval\n");
        description.append("- **Validation**: Input validation and error handling\n");
        description.append("- **Security**: JWT and Basic authentication support\n");
        {{#hasCamel}}
        description.append("- **Integration**: Apache Camel routes for enterprise integration\n");
        {{/hasCamel}}
        {{#hasMonitoring}}
        description.append("- **Monitoring**: Health checks and metrics\n");
        {{/hasMonitoring}}
        description.append("\n");

        description.append("## Authentication\n");
        description.append("This API supports two authentication methods:\n");
        description.append("1. **Bearer Token (JWT)**: Include `Authorization: Bearer <token>` header\n");
        description.append("2. **Basic Authentication**: Include `Authorization: Basic <credentials>` header\n\n");

        description.append("## Error Handling\n");
        description.append("The API uses standard HTTP status codes and provides detailed error responses:\n");
        description.append("- `200` - Success\n");
        description.append("- `201` - Created\n");
        description.append("- `400` - Bad Request (validation errors)\n");
        description.append("- `401` - Unauthorized\n");
        description.append("- `403` - Forbidden\n");
        description.append("- `404` - Not Found\n");
        description.append("- `409` - Conflict (duplicate resources)\n");
        description.append("- `500` - Internal Server Error\n\n");

        description.append("## Rate Limiting\n");
        description.append("API requests are rate limited to ensure fair usage:\n");
        description.append("- **Authenticated users**: 1000 requests per hour\n");
        description.append("- **Public endpoints**: 100 requests per hour\n\n");

        description.append("## Versioning\n");
        description.append("This API follows semantic versioning. Breaking changes will increment the major version.\n");
        description.append("Current version: ").append(applicationVersion).append("\n\n");

        description.append("## Support\n");
        description.append("For API support, please contact: api-support@company.com\n");
        description.append("Documentation: https://docs.company.com/api\n");

        return description.toString();
    }
}
{{/hasOpenApi}}
{{^hasOpenApi}}
// OpenAPI documentation is not enabled for this microservice
// To enable API documentation, regenerate with hasOpenApi=true option
{{/hasOpenApi}}