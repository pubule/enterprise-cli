package {{packageName}}.monitoring;

import {{packageName}}.dto.{{domainTitleCase}}Response;
import {{packageName}}.service.{{domainTitleCase}}Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Component;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

/**
 * Custom Health Monitor for {{domainTitleCase}} Service
 *
 * Monitors the health of {{domainTitleCase}} business operations and data integrity.
 * Generated by Enterprise CLI - customize according to your business requirements.
 */
@Component("{{domain}}Health")
public class {{domainTitleCase}}HealthIndicator {

    private final {{domainTitleCase}}Service {{domain}}Service;

    @Autowired
    public {{domainTitleCase}}HealthIndicator({{domainTitleCase}}Service {{domain}}Service) {
        this.{{domain}}Service = {{domain}}Service;
    }

    public java.util.Map<String, Object> checkHealth() {
        try {
            // Check if service is responsive
            long startTime = System.currentTimeMillis();

            // Basic connectivity test
            Page<{{domainTitleCase}}Response> totalPage = {{domain}}Service.findAll(Pageable.unpaged());
            long totalCount = totalPage.getTotalElements();

            long responseTime = System.currentTimeMillis() - startTime;

            // Check response time (should be under 1 second)
            if (responseTime > 1000) {
                java.util.Map<String, Object> result = new java.util.HashMap<>();
                result.put("status", "SLOW_RESPONSE");
                result.put("responseTimeMs", responseTime);
                result.put("threshold", "1000ms");
                result.put("message", "{{domainTitleCase}} service is responding slowly");
                result.put("timestamp", getCurrentTimestamp());
                return result;
            }

            // totalCount already calculated above

            // Check for recent activity (basic business logic validation)
            LocalDateTime oneHourAgo = LocalDateTime.now().minusHours(1);
            long recentCount = {{domain}}Service.count{{domainTitleCase}}sCreatedAfter(oneHourAgo);

            // Additional health checks can be added here:
            // - Database connectivity
            // - External service dependencies
            // - Cache availability
            // - Message queue connectivity

            java.util.Map<String, Object> result = new java.util.HashMap<>();
            result.put("status", "HEALTHY");
            result.put("total{{domainTitleCase}}s", totalCount);
            result.put("recent{{domainTitleCase}}s", recentCount);
            result.put("responseTimeMs", responseTime);
            result.put("timestamp", getCurrentTimestamp());
            result.put("service", "{{domainTitleCase}} Service");
            result.put("version", "1.0.0");
            return result;

        } catch (Exception e) {
            java.util.Map<String, Object> result = new java.util.HashMap<>();
            result.put("status", "ERROR");
            result.put("error", e.getClass().getSimpleName());
            result.put("message", e.getMessage());
            result.put("timestamp", getCurrentTimestamp());
            result.put("service", "{{domainTitleCase}} Service");
            return result;
        }
    }

    private String getCurrentTimestamp() {
        return LocalDateTime.now().format(DateTimeFormatter.ISO_LOCAL_DATE_TIME);
    }
}