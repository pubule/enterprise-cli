package {{packageName}}.monitoring;

import io.micrometer.core.instrument.Counter;
import io.micrometer.core.instrument.Gauge;
import io.micrometer.core.instrument.MeterRegistry;
import io.micrometer.core.instrument.Timer;
import {{packageName}}.dto.{{domainTitleCase}}Response;
import {{packageName}}.service.{{domainTitleCase}}Service;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;

import jakarta.annotation.PostConstruct;
import java.time.LocalDateTime;
import java.util.concurrent.atomic.AtomicLong;

/**
 * Custom Metrics Service for {{domainTitleCase}} Operations
 *
 * Provides business-specific metrics for monitoring and alerting.
 * Generated by Enterprise CLI - customize according to your business requirements.
 */
@Service
public class {{domainTitleCase}}MetricsService {

    private final MeterRegistry meterRegistry;
    private final {{domainTitleCase}}Service {{domain}}Service;

    // Counters for tracking operations
    private Counter {{domain}}CreatedCounter;
    private Counter {{domain}}UpdatedCounter;
    private Counter {{domain}}DeletedCounter;
    private Counter {{domain}}SearchCounter;
    private Counter {{domain}}ErrorCounter;

    // Timers for tracking performance
    private Timer {{domain}}CreationTimer;
    private Timer {{domain}}UpdateTimer;
    private Timer {{domain}}SearchTimer;
    private Timer {{domain}}DatabaseQueryTimer;

    // Gauges for current state
    private AtomicLong total{{domainTitleCase}}Count = new AtomicLong(0);
    private AtomicLong active{{domainTitleCase}}Count = new AtomicLong(0);

    @Autowired
    public {{domainTitleCase}}MetricsService(MeterRegistry meterRegistry, {{domainTitleCase}}Service {{domain}}Service) {
        this.meterRegistry = meterRegistry;
        this.{{domain}}Service = {{domain}}Service;
    }

    @PostConstruct
    public void initializeMetrics() {
        // Initialize counters
        {{domain}}CreatedCounter = Counter.builder("{{domain}}.created")
                .description("Number of {{domain}}s created")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}UpdatedCounter = Counter.builder("{{domain}}.updated")
                .description("Number of {{domain}}s updated")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}DeletedCounter = Counter.builder("{{domain}}.deleted")
                .description("Number of {{domain}}s deleted")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}SearchCounter = Counter.builder("{{domain}}.searched")
                .description("Number of {{domain}} searches performed")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}ErrorCounter = Counter.builder("{{domain}}.errors")
                .description("Number of {{domain}} operation errors")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        // Initialize timers
        {{domain}}CreationTimer = Timer.builder("{{domain}}.creation.time")
                .description("Time taken to create a {{domain}}")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}UpdateTimer = Timer.builder("{{domain}}.update.time")
                .description("Time taken to update a {{domain}}")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}SearchTimer = Timer.builder("{{domain}}.search.time")
                .description("Time taken to search {{domain}}s")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        {{domain}}DatabaseQueryTimer = Timer.builder("{{domain}}.database.query.time")
                .description("Time taken for database queries")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        // Initialize gauges
        Gauge.builder("{{domain}}.total.count", this, {{domainTitleCase}}MetricsService::getTotalCount)
                .description("Total number of {{domain}}s in the system")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        Gauge.builder("{{domain}}.active.count", this, {{domainTitleCase}}MetricsService::getActiveCount)
                .description("Number of active {{domain}}s")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        Gauge.builder("{{domain}}.recent.count", this, {{domainTitleCase}}MetricsService::getRecentCount)
                .description("Number of {{domain}}s created in the last hour")
                .tag("service", "{{serviceName}}")
                .register(meterRegistry);

        // Initialize current counts
        updateCounts();
    }

    // Metric recording methods

    public void recordCreation() {
        {{domain}}CreatedCounter.increment();
        updateCounts();
    }

    public void recordUpdate() {
        {{domain}}UpdatedCounter.increment();
        updateCounts();
    }

    public void recordDeletion() {
        {{domain}}DeletedCounter.increment();
        updateCounts();
    }

    public void recordSearch() {
        {{domain}}SearchCounter.increment();
    }

    public void recordError() {
        {{domain}}ErrorCounter.increment();
    }

    public Timer.Sample startCreationTimer() {
        return Timer.start(meterRegistry);
    }

    public void stopCreationTimer(Timer.Sample sample) {
        sample.stop({{domain}}CreationTimer);
    }

    public Timer.Sample startUpdateTimer() {
        return Timer.start(meterRegistry);
    }

    public void stopUpdateTimer(Timer.Sample sample) {
        sample.stop({{domain}}UpdateTimer);
    }

    public Timer.Sample startSearchTimer() {
        return Timer.start(meterRegistry);
    }

    public void stopSearchTimer(Timer.Sample sample) {
        sample.stop({{domain}}SearchTimer);
    }

    public Timer.Sample startDatabaseQueryTimer() {
        return Timer.start(meterRegistry);
    }

    public void stopDatabaseQueryTimer(Timer.Sample sample) {
        sample.stop({{domain}}DatabaseQueryTimer);
    }

    // Gauge value providers

    private double getTotalCount() {
        return total{{domainTitleCase}}Count.get();
    }

    private double getActiveCount() {
        return active{{domainTitleCase}}Count.get();
    }

    private double getRecentCount() {
        try {
            LocalDateTime oneHourAgo = LocalDateTime.now().minusHours(1);
            return {{domain}}Service.count{{domainTitleCase}}sCreatedAfter(oneHourAgo);
        } catch (Exception e) {
            return 0;
        }
    }

    // Business metrics

    public void recordBusinessMetric(String operation, double value) {
        meterRegistry.gauge("{{domain}}.business." + operation, value);
    }

    public void recordPerformanceMetric(String operation, long duration) {
        Timer.builder("{{domain}}.performance." + operation)
                .tag("service", "{{serviceName}}")
                .register(meterRegistry)
                .record(duration, java.util.concurrent.TimeUnit.MILLISECONDS);
    }

    // Helper methods

    private void updateCounts() {
        try {
            // Update total count
            Page<{{domainTitleCase}}Response> totalPage = {{domain}}Service.findAll(Pageable.unpaged());
            long total = totalPage.getTotalElements();
            total{{domainTitleCase}}Count.set(total);

            // Update active count ({{domain}}s with descriptions - example business rule)
            long active = {{domain}}Service.getActive{{domainTitleCase}}s().size();
            active{{domainTitleCase}}Count.set(active);

        } catch (Exception e) {
            // Log error but don't fail the application
            recordError();
        }
    }

    // Custom business metrics

    public void recordUserEngagement(String userId, String operation) {
        Counter.builder("{{domain}}.user.engagement")
                .description("User engagement with {{domain}} operations")
                .tag("service", "{{serviceName}}")
                .tag("operation", operation)
                .tag("user", userId)
                .register(meterRegistry)
                .increment();
    }

    public void recordAPIUsage(String endpoint, String method, int statusCode) {
        Counter.builder("{{domain}}.api.usage")
                .description("API endpoint usage statistics")
                .tag("service", "{{serviceName}}")
                .tag("endpoint", endpoint)
                .tag("method", method)
                .tag("status", String.valueOf(statusCode))
                .register(meterRegistry)
                .increment();
    }

    public void recordCacheHitRate(String cacheOperation, boolean hit) {
        Counter.builder("{{domain}}.cache." + (hit ? "hits" : "misses"))
                .description("Cache hit/miss statistics")
                .tag("service", "{{serviceName}}")
                .tag("operation", cacheOperation)
                .register(meterRegistry)
                .increment();
    }
}