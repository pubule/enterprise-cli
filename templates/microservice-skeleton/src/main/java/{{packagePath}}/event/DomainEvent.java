package {{packageName}}.event;

import java.time.LocalDateTime;
import java.util.UUID;

/**
 * Base domain event for {{serviceNameTitleCase}}
 *
 * Provides foundation for event-driven architecture and decoupled business logic.
 * Generated by Enterprise CLI - extend for specific domain events.
 */
public abstract class DomainEvent {

    private final String eventId;
    private final LocalDateTime occurredAt;
    private final String eventType;
    private final String aggregateId;
    private final Long aggregateVersion;

    protected DomainEvent(String eventType, String aggregateId, Long aggregateVersion) {
        this.eventId = UUID.randomUUID().toString();
        this.occurredAt = LocalDateTime.now();
        this.eventType = eventType;
        this.aggregateId = aggregateId;
        this.aggregateVersion = aggregateVersion;
    }

    // Getters
    public String getEventId() { return eventId; }
    public LocalDateTime getOccurredAt() { return occurredAt; }
    public String getEventType() { return eventType; }
    public String getAggregateId() { return aggregateId; }
    public Long getAggregateVersion() { return aggregateVersion; }

    @Override
    public String toString() {
        return "DomainEvent{" +
                "eventId='" + eventId + '\'' +
                ", occurredAt=" + occurredAt +
                ", eventType='" + eventType + '\'' +
                ", aggregateId='" + aggregateId + '\'' +
                ", aggregateVersion=" + aggregateVersion +
                '}';
    }
}