package {{packageName}}.event;

import {{packageName}}.entity.{{domainTitleCase}};

/**
 * Domain event fired when a {{domainTitleCase}} is updated
 *
 * Enables loose coupling between {{domain}} updates and other business processes.
 * Generated by Enterprise CLI - customize with additional data as needed.
 */
public class {{domainTitleCase}}UpdatedEvent extends DomainEvent {

    private final String {{domain}}Id;
    private final String previousName;
    private final String newName;
    private final String previousDescription;
    private final String newDescription;

    public {{domainTitleCase}}UpdatedEvent({{domainTitleCase}} previous{{domainTitleCase}}, {{domainTitleCase}} updated{{domainTitleCase}}) {
        super("{{domainTitleCase}}Updated", updated{{domainTitleCase}}.getId(), updated{{domainTitleCase}}.getVersion());
        this.{{domain}}Id = updated{{domainTitleCase}}.getId();
        this.previousName = previous{{domainTitleCase}}.getName();
        this.newName = updated{{domainTitleCase}}.getName();
        this.previousDescription = previous{{domainTitleCase}}.getDescription();
        this.newDescription = updated{{domainTitleCase}}.getDescription();
    }

    // Getters
    public String get{{domainTitleCase}}Id() { return {{domain}}Id; }
    public String getPreviousName() { return previousName; }
    public String getNewName() { return newName; }
    public String getPreviousDescription() { return previousDescription; }
    public String getNewDescription() { return newDescription; }

    public boolean isNameChanged() {
        return !java.util.Objects.equals(previousName, newName);
    }

    public boolean isDescriptionChanged() {
        return !java.util.Objects.equals(previousDescription, newDescription);
    }

    @Override
    public String toString() {
        return "{{domainTitleCase}}UpdatedEvent{" +
                "{{domain}}Id='" + {{domain}}Id + '\'' +
                ", previousName='" + previousName + '\'' +
                ", newName='" + newName + '\'' +
                ", previousDescription='" + previousDescription + '\'' +
                ", newDescription='" + newDescription + '\'' +
                ", " + super.toString() +
                '}';
    }
}